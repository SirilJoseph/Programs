#include<stdio.h>
 
int main() 
{
   float s, a;
   printf("\nEnter the Length of Side : ");
   scanf("%f", &s);
   a=s*s;//area=side*side
   printf("\nArea of Square : %f", a);
   return 0;
}
